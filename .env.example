# Local dev (optional — defaults in src/shared/config.ts)
# PORT=4446
# PGHOST=localhost
# PGDATABASE=heatrip
# PGUSER=postgres
# PGPASSWORD=

# Railway: set DATABASE_URL from the Postgres service (replaces the above).
